{
  "name": "video-downloader-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev:backend & npm run dev:web",
    "dev:all": "npm run dev:backend & npm run dev:web & npm run dev:mobile",
    "dev:backend": "npm --prefix backend run dev",
    "dev:web": "npm --prefix web run dev",
    "dev:mobile": "npm --prefix mobile run dev",
    "install:all": "npm --prefix backend install && npm --prefix web install && npm --prefix mobile install",
    "build": "npm --prefix web install && npm --prefix web run build"
  }
}
